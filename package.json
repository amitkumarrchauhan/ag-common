{
  "name": "ag-common",
  "private": false,
  "dependencies": {},
  "scripts": {
    "lerna-version": "./node_modules/.bin/lerna version --no-private",
    "build-util": "cd packages/utils && pnpm run build",
    "publish-util": "cd packages/utils && pnpm run publish"
  },
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
